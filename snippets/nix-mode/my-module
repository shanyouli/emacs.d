# -*- mode: snippet -*-
# name: my-module nix
# key: mx
# --
{pkgs, lib, config, options, ...}:
with lib;
with lib.my;
let
  cfm = config.modules;
  cfg = cfm.$1;
in {
  options.modules.$1 = {
    enable = mkEnableOption "$2";
    $3
  };
  config = mkIf cfg.enable {
    $0
  };
}
